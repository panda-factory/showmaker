SET(TARGET_NAME css)
SET(CSS_SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})

SET(CSS_SRC
    ${CSS_SRC_DIR}/color.cpp
    ${CSS_SRC_DIR}/color.h)

ADD_LIBRARY(${TARGET_NAME} STATIC ${CSS_SRC})

TARGET_INCLUDE_DIRECTORIES(${TARGET_NAME} PUBLIC)

TARGET_LINK_LIBRARIES(${TARGET_NAME} PUBLIC)
